import SwiftUI

enum UIConstants {
    enum Typography {
        static let header = Font.system(size: 16, weight: .bold)
        static let sectionLabel = Font.system(size: 14, weight: .semibold)
        static let regular = Font.system(size: 12, weight: .regular)
    }
}
